#include <stdio.h>

char* madlib_by_numbers(char* template, int word_count, char* words[]){
	for(int i = 0; i< strlen(template); i+=1){
		if(isdigits template[i])
	}
}

int* 

int main(){
	return 0;
}